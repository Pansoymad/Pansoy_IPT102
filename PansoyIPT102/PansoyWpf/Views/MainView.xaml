<UserControl x:Class="PansoyWpf.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PansoyWpf.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="LightBlue">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="370"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Content="MOVIE MANAGER" Grid.ColumnSpan="2" HorizontalAlignment="Center" VerticalAlignment="Top" FontSize="36" Margin="0,6,0,0"/>

        <!-- Left: inputs & buttons -->
        <StackPanel Grid.Column="0" Orientation="Vertical" HorizontalAlignment="Left" Width="363" Margin="10,72,0,0" VerticalAlignment="Top" Height="220">
            <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                <Label Content="Title" Width="80" FontSize="16"/>
                <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" Width="250" Height="26"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                <Label Content="Director" Width="80" FontSize="16"/>
                <TextBox Text="{Binding Director, UpdateSourceTrigger=PropertyChanged}" Width="250" Height="26"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                <Label Content="Year" Width="80" FontSize="16"/>
                <TextBox Text="{Binding Year, UpdateSourceTrigger=PropertyChanged}" Width="120" Height="26"/>
                <Label Content="Genre" Width="60" FontSize="16" Margin="10,0,0,0"/>
                <TextBox Text="{Binding Genre, UpdateSourceTrigger=PropertyChanged}" Width="120" Height="26"/>
            </StackPanel>

            <!-- Buttons area -->
            <WrapPanel Margin="0,12,0,0" HorizontalAlignment="Left">
                <Button Content="Add" Width="80" Height="30" Margin="0,0,8,8" Command="{Binding AddCommand}"/>
                <Button Content="Edit" Width="80" Height="30" Margin="0,0,8,8" Command="{Binding EditCommand}"/>
                <Button Content="Update" Width="80" Height="30" Margin="0,0,8,8" Command="{Binding UpdateCommand}"/>
                <Button Content="Cancel" Width="80" Height="30" Margin="0,0,8,8" Command="{Binding CancelCommand}"/>
            </WrapPanel>
        </StackPanel>

        <!-- Right: search bar, list + Delete button -->
        <StackPanel Grid.Column="1" Margin="10,72,10,10">
            <!-- Search Bar -->
            <Border Background="White" CornerRadius="8" Padding="10,8" Margin="0,0,0,10" BorderBrush="DarkGray" BorderThickness="1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBox Grid.Column="0" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                             FontSize="14" BorderThickness="0" VerticalContentAlignment="Center"
                             Background="Transparent">
                        <TextBox.Style>
                            <Style TargetType="TextBox">
                                <Style.Triggers>
                                    <Trigger Property="Text" Value="">
                                        <Setter Property="Background">
                                            <Setter.Value>
                                                <VisualBrush Stretch="None" AlignmentX="Left">
                                                    <VisualBrush.Visual>
                                                        <TextBlock Text="Search movies..." Foreground="LightGray" FontSize="14"/>
                                                    </VisualBrush.Visual>
                                                </VisualBrush>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </TextBox.Style>
                    </TextBox>
                    
                    <TextBlock Grid.Column="1" Text="🔍" FontSize="16" VerticalAlignment="Center" Margin="5,0,0,0"/>
                </Grid>
            </Border>
            
            <ListBox ItemsSource="{Binding FilteredMovies}" SelectedItem="{Binding SelectedMovie}" Height="300">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Padding="10" Margin="4" Background="White" CornerRadius="6" BorderBrush="LightGray" BorderThickness="1">
                            <StackPanel>
                                <TextBlock FontSize="16" FontWeight="Bold" Margin="0,0,0,8">
                                    <Run Text="Title: " Foreground="DarkBlue"/>
                                    <Run Text="{Binding Title}" Foreground="Black"/>
                                </TextBlock>
                                <TextBlock FontSize="14" Margin="0,2,0,0">
                                    <Run Text="Director: " FontWeight="SemiBold" Foreground="DarkGreen"/>
                                    <Run Text="{Binding Director}" Foreground="Gray"/>
                                </TextBlock>
                                <TextBlock FontSize="14" Margin="0,2,0,0">
                                    <Run Text="Genre: " FontWeight="SemiBold" Foreground="DarkOrange"/>
                                    <Run Text="{Binding Genre}" Foreground="Gray"/>
                                </TextBlock>
                                <TextBlock FontSize="14" Margin="0,2,0,0">
                                    <Run Text="Year: " FontWeight="SemiBold" Foreground="DarkRed"/>
                                    <Run Text="{Binding Year}" Foreground="Gray"/>
                                </TextBlock>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
                <Button Content="Delete" Width="100" Height="32" Command="{Binding DeleteCommand}"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
