<UserControl x:Class="PansoyWpf.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="LightBlue">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="20,30,20,20">
            <TextBlock Text="MOVIE MANAGER" FontSize="48" FontWeight="Bold" 
                       HorizontalAlignment="Center" Foreground="DarkBlue"/>
            <TextBlock Text="Search and manage your movie collection" FontSize="16" 
                       HorizontalAlignment="Center" Foreground="Gray" Margin="0,10,0,0"/>
        </StackPanel>

        <!-- Search Bar -->
        <Border Grid.Row="1" Background="White" CornerRadius="25" Padding="15,10" 
                Margin="50,20,50,20" BorderBrush="DarkGray" BorderThickness="2">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBox Grid.Column="0" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                         FontSize="18" BorderThickness="0" VerticalContentAlignment="Center"
                         Background="Transparent">
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Style.Triggers>
                                <Trigger Property="Text" Value="">
                                    <Setter Property="Background">
                                        <Setter.Value>
                                            <VisualBrush Stretch="None" AlignmentX="Left">
                                                <VisualBrush.Visual>
                                                    <TextBlock Text="Search movies by title, director, or genre..." 
                                                               Foreground="LightGray" FontSize="18"/>
                                                </VisualBrush.Visual>
                                            </VisualBrush>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>
                
                <Button Grid.Column="1" Content="🔍" FontSize="20" Width="40" Height="40" 
                        Background="DarkBlue" Foreground="White" BorderThickness="0" 
                        Cursor="Hand" Command="{Binding SearchCommand}"/>
            </Grid>
        </Border>

        <!-- Movie Results -->
        <ScrollViewer Grid.Row="2" Margin="50,10,50,30" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding FilteredMovies}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Padding="15" Margin="0,0,0,10" Background="White" 
                                CornerRadius="10" BorderBrush="LightGray" BorderThickness="1"
                                Cursor="Hand">
                            <Border.InputBindings>
                                <MouseBinding MouseAction="LeftClick" 
                                              Command="{Binding DataContext.ViewMovieCommand, 
                                              RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                              CommandParameter="{Binding}"/>
                            </Border.InputBindings>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <StackPanel Grid.Column="0">
                                    <TextBlock FontSize="20" FontWeight="Bold" Text="{Binding Title}" 
                                               Foreground="DarkBlue"/>
                                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                        <TextBlock Text="Director: " FontWeight="SemiBold" Foreground="Gray"/>
                                        <TextBlock Text="{Binding Director}" Foreground="Gray"/>
                                        <TextBlock Text="  •  " Foreground="Gray" Margin="8,0"/>
                                        <TextBlock Text="Genre: " FontWeight="SemiBold" Foreground="Gray"/>
                                        <TextBlock Text="{Binding Genre}" Foreground="Gray"/>
                                        <TextBlock Text="  •  " Foreground="Gray" Margin="8,0"/>
                                        <TextBlock Text="Year: " FontWeight="SemiBold" Foreground="Gray"/>
                                        <TextBlock Text="{Binding Year}" Foreground="Gray"/>
                                    </StackPanel>
                                </StackPanel>
                                
                                <TextBlock Grid.Column="1" Text="→" FontSize="24" 
                                           VerticalAlignment="Center" Foreground="DarkBlue"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
